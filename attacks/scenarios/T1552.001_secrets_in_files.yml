technique: T1552.001
description: Recursively grep /etc and /home for credential strings
runner: script
command: |
  grep -rniE 'password|api_key|secret|token' /etc /root 2>/dev/null | head -n 5 || true
expect:
  engine: falco
  rule: DLL Credential Search
