technique: T1136.001
description: Create a local account with useradd, then delete it
runner: script
command: |
  useradd -m -s /bin/bash dll_backdoor ; id dll_backdoor ; userdel -r dll_backdoor
expect:
  engine: wazuh
  rule_id: "100313"
