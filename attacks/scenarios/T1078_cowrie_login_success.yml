technique: T1078
description: Log into the Cowrie honeypot with a valid (lab-seeded) credential
runner: script
command: |
  sshpass -p "dllpass123" ssh -p 2222 -o StrictHostKeyChecking=no \
    -o UserKnownHostsFile=/dev/null -o ConnectTimeout=5 dll@cowrie exit 2>/dev/null || true
expect:
  engine: wazuh
  rule_id: "100511"
